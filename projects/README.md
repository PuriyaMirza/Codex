# Projects

Use this folder for small, self-contained apps and projects.

Each project should have its own folder and README. For example:

```text
projects/
├── job-posting-collector/
├── translator-app/
└── activity-wheel/
```

When a project becomes a serious standalone product or portfolio piece, consider moving it into its own GitHub repository.
